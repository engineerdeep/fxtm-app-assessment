name: forex_repository
description: "A Dart repository used to fetch forex data."
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  finnhub_api:
    path: ../finnhub_api

dev_dependencies:
  flutter_lints: ^5.0.0
  mocktail: ^1.0.4
  test: ^1.25.15
